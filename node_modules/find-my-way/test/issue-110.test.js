'use strict'

const t = require('tap')
const test = t.test
const FindMyWay = require('../')

test('Nested static parametric route, url with parameter common prefix > 1', t => {
  t.plan(1)
  const findMyWay = FindMyWay({
    defaultRoute: (req, res) => {
      t.fail('Should not be defaultRoute')
    }
  })

  findMyWay.on('GET', '/api/bar/b2', (req, res) => {
    res.end('{"message":"hello world"}')
  })

  findMyWay.on('GET', '/api/bar/bar/qux', (req, res) => {
    res.end('{"message":"hello world"}')
  })

  findMyWay.on('GET', '/api/bar/:id/bar', (req, res) => {
    res.end('{"message":"hello world"}')
  })

  findMyWay.on('GET', '/bar', (req, res) => {
    res.end('{"message":"hello world"}')
  })

  t.same(findMyWay.find('GET', '/api/bar/b-123/bar').params, { id: 'b-123' })
})
