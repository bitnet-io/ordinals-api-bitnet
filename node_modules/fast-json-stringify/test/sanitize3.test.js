'use strict'

const t = require('tap')
const build = require('..')

t.throws(() => {
  build({
    $defs: {
      type: 'baroo"bar'
    },
    patternProperties: {
      x: { $ref: '#/$defs' }
    }
  })
}, 'baroo"bar unsupported')
