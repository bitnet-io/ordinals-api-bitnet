import t from 'tap'
import Fastify from '../../fastify.js'

t.test('esm support', async t => {
  const fastify = Fastify()

  fastify.register(import('./plugin.mjs'), { bar: 'bar' })
  fastify.register(import('./other.mjs'))

  await fastify.ready()

  t.equal(fastify.bar, 'bar')
})
